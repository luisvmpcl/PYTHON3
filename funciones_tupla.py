#18
tupla =(1,2,3,4,5,6,7,8,9,6,6,6,6,6,6,6,6,6,6,6)
print(tupla.index(4)) # con esto imprime la posicion del elemento
print(len(tupla)) # para saber el tamaño de la tupla o cantidad de elementos que tengamos
print(tupla.count(6)) # para saber cuanta veces se repite un elemento
#del tupla  # para eliminar una tupla , claro nos va salir un error pero lo elimina
tupla = () #con esto estamos haciendo una redeclaracion,  estamos quitando los elementos pero no borrando la variable
print(tupla)